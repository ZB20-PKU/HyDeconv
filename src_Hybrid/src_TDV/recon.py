import os
import torch
import numpy as np
from src_TDV.basic import *
from src.ImageLib import ImageLib


class Recon:
    def __init__(self, args):
        self.device = args.device

        # self.savedir = args.savedir

        # self.imgsize = args.imgsize

    # 重建
    def recon(self, **kwargs):
        pass
